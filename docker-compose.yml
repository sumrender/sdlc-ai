services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5959:5432"
    environment:
      POSTGRES_USER: sdlc
      POSTGRES_PASSWORD: sdlc
      POSTGRES_DB: sdlc
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
